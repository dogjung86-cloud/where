# Somewhere Agent Notes

This app is the initial `Somewhere` / `$WHERE` MVP.

- Use Railway for the Next.js app, API routes, workers, and deployment.
- Use Supabase for Postgres, Auth, and private photo storage.
- Keep exact GPS private. Store city/country display data and rounded coordinates only when needed.
- Images should be EXIF-stripped and normalized before delivery.
- Token spend policy starts as 60% burn, 30% marketing treasury, 10% rewards.
